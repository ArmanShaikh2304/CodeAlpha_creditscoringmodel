# Data

This project uses the public UCI Statlog German Credit dataset.

Running `python preprocess.py` or `python train.py` downloads the raw dataset into this
folder and creates `credit_scoring_processed.csv` with the cleaned, engineered
features used by the model.

